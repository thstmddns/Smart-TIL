1. 로그인
   1. 로그인시 아이디, 비밀번호가 입력이 되지 않으면 로그인 실패
      1. 자바 스크립트로 체크/실행
   2. onsubmit
      1. form태그 안에 입력하며 onsubmit의 값이 true면 action주소로 이동
2. Cookie
3.  DB
4. File
5. Session
   1. 정보는 웹페이지에 한정되기에 만약, 사용자가 다른 페이지에 접속하면 각 페이지마다 정보를 계산해줘야 한다.
   2. 하지만 세션에 넣어주면 접속한 페이지와 상관없이 세션에서 정보를 불러오기만 하면 된다.
   3. 로그아웃
      1. session 객체를 지운다
      2. 세션에 보관된 변수가 지워지고 새로운 session개체가 할당된다.
6. @Controller 
   1. 모델 뷰를 리턴해준다.
   2. ModelAndView
7. @RestController
   1. 모델이 리턴된다.